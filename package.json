{
  "name": "part-sync",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node --no-warnings --experimental-sqlite server.js",
    "build": "vite build",
    "start": "node --no-warnings --experimental-sqlite server.js --prod",
    "preview": "npm run build && npm run start"
  },
  "devDependencies": {
    "vite": "^5.2.11"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
